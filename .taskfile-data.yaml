---
# .taskfile-data.yaml
# Optional configuration for dr task compose / `task:dev` (drdev)

# Development server ports — order matters (started top to bottom).
# Full local stack for dashboard + chat (agent needs MCP for tools).
ports:
  - name: fastapi_server
    port: 8080
    print_url: true
  - name: frontend_web
    port: 5173
    print_url: true
  - name: agent
    port: 8842
    print_url: true
  - name: mcp_server
    port: 9000
